/**
 * Generated by orval v8.4.0 🍺
 * Do not edit manually.
 * Vouchsafe API docs
 * Vouchsafe offers a REST API to conduct operations programmatically.

You can request and manage verifications, as well as perform smart lookups.

To use the API, you need a client ID and secret from the Vouchsafe dashboard. [Sign up here](https://app.vouchsafe.id/) if you haven't already.

We also have a [video guide](https://www.youtube.com/playlist?list=PLx6V6SSTMuF_ZNWBPnysvwmdIwboLViE8) showing the basics, as well as [SDKs and libraries](https://help.vouchsafe.id/en/articles/12026847-vouchsafe-sdks-and-libraries) for popular languages.
 * OpenAPI spec version: 0.1.0
 */
import type { ValidationOutcomeApi } from './validationOutcomeApi';

export interface SupportingDocumentValidationsApi {
  /** Whether required fields were extracted. */
  required_fields_extracted: ValidationOutcomeApi;
  /** Whether the uploaded document is an accepted category for this flow. */
  category_match?: ValidationOutcomeApi;
  /** Whether the document age is within the allowed validity period (when applicable). */
  document_age_valid?: ValidationOutcomeApi;
  /** Whether this document is unique when multiple documents are uploaded. */
  document_unique?: ValidationOutcomeApi;
  /** Whether date fields are valid (for example DOB or document date). */
  date_fields_valid?: ValidationOutcomeApi;
  /** Whether names match other evidence (when applicable). */
  names_consistent?: ValidationOutcomeApi;
  /** Whether addresses match other evidence (when the document contains an address). */
  addresses_consistent?: ValidationOutcomeApi;
  /** Whether dates of birth match other evidence (for example for birth certificates). */
  dates_of_birth_consistent?: ValidationOutcomeApi;
  /** Whether the referee's name differs from the user (when applicable). */
  referee_biometrics_dissimilar?: ValidationOutcomeApi;
  /** Whether the document is complete and readable. Set only after required checks pass. */
  document_complete_and_legible?: ValidationOutcomeApi;
  /** Whether tampering signals were found. Set only after required checks pass. */
  tampering_signals_check?: ValidationOutcomeApi;
}
