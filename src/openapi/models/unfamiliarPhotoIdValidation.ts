/**
 * Generated by orval v8.4.0 🍺
 * Do not edit manually.
 * Vouchsafe API docs
 * Vouchsafe offers a REST API to conduct operations programmatically.

You can request and manage verifications, as well as perform smart lookups.

To use the API, you need a client ID and secret from the Vouchsafe dashboard. [Sign up here](https://app.vouchsafe.id/) if you haven't already.

We also have a [video guide](https://www.youtube.com/playlist?list=PLx6V6SSTMuF_ZNWBPnysvwmdIwboLViE8) showing the basics, as well as [SDKs and libraries](https://help.vouchsafe.id/en/articles/12026847-vouchsafe-sdks-and-libraries) for popular languages.
 * OpenAPI spec version: 0.1.0
 */
import type { CheckDocumentFaceErrorCode } from './checkDocumentFaceErrorCode';
import type { CheckSpecimenErrorCode } from './checkSpecimenErrorCode';
import type { CheckTamperingErrorCode } from './checkTamperingErrorCode';
import type { CheckUnfamiliarPhotoIdFailedReason } from './checkUnfamiliarPhotoIdFailedReason';
import type { ScoreCheckDocumentDimensionsErrorCode } from './scoreCheckDocumentDimensionsErrorCode';
import type { ScoreCheckFaceMatchErrorCode } from './scoreCheckFaceMatchErrorCode';

export interface UnfamiliarPhotoIdValidation {
  /** Whether the required fields (first_name, last_name, document_number) were successfully extracted. */
  required_fields_extracted: CheckUnfamiliarPhotoIdFailedReason;
  /** Whether the document has not passed its expiry date. Absent if no expiry date was found. */
  has_not_expired?: CheckUnfamiliarPhotoIdFailedReason;
  /** Whether the document appears to be a specimen or sample rather than a real issued document. */
  not_specimen: CheckSpecimenErrorCode;
  /** Whether the document appears to be a screenshot or has moire patterns indicating a photographed screen. */
  not_screenshot: CheckTamperingErrorCode;
  /** Whether the document dimensions matched the expected dimensions for the document type. */
  document_dimensions_matched: ScoreCheckDocumentDimensionsErrorCode;
  /** Whether a face was detected within the document's bounding box. */
  document_contains_face: CheckDocumentFaceErrorCode;
  /** Whether the document photo matched the face scan. Absent if no face scan was provided. */
  face_match?: ScoreCheckFaceMatchErrorCode;
}
